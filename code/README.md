Contains GUI/interface design files

How to run:
1. Run 'node -v' to make sure you have node.js installed, if not install it and run command again to check

2. Run 'npm install -g create-vite'

3. To run application, cd into the 'my-react-app' folder and type 'npm run dev'